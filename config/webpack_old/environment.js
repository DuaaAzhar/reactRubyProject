const { environment } = require('@rails/webpacker')

const customConfig = {
    // plugin: {
    //     new WasmPackPlugin({
    //         crateDirectory: __dirname,
    //       }),
    // }
    
}

module.exports = environment
